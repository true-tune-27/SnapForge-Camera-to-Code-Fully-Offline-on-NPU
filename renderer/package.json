{
  "name": "@snapforge/renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic layout JSON → TSX renderer. Shared phone + desktop.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "forge": "tsx src/cli.ts"
  },
  "dependencies": {
    "typescript": "~5.6.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "tsx": "^4.19.0",
    "@snapforge/schema": "*"
  }
}
